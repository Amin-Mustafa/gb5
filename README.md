# gb5 - Gameboy Emulator in C++

## Building
Requires gcc 8 or higher. If you have an installation of make, download the zip file and call 
    ```
    make
    ```
to build.
You could also manually compile with gcc directly, but make sure to use 
    ```
    -std=c++20
    ```